---
alwaysApply: true
description: Règle pour gérer la création automatique de tâches dans la roadmap centralisée lorsque des tâches sont identifiées pour le futur
---

# Règle Agent — Gestion de la Roadmap Centralisée 📋

## Objectif

Cette règle t'explique **quand** et **comment** créer des tâches dans la roadmap centralisée (`.cursor/agents/roadmap.yaml`) lorsque tu identifies des travaux à faire dans le futur ou qui peuvent être effectués en parallèle.

## Principe Fondamental

Le système de roadmap permet de :
- Coordonner plusieurs agents Cursor travaillant en parallèle
- Garder une trace centralisée de toutes les tâches à faire
- Permettre à chaque agent de consulter le travail accompli par les autres via les fichiers de résultats
- Faciliter le saut d'un chat à l'autre tout en gardant le contexte

## Quand Créer une Tâche dans la Roadmap

Tu **DOIS** créer une tâche dans la roadmap lorsque :

1. **Tâche mentionnée pour le futur** : Une tâche est mentionnée ou identifiée qui ne sera **certainement pas** effectuée dans le chat actuel
2. **Tâche parallélisable** : Une tâche peut être effectuée en parallèle avec d'autres travaux en cours
3. **Dépendance future** : Une tâche doit être faite plus tard mais dépend de travaux actuels

Tu **NE DOIS PAS** créer de tâche si :
- La tâche sera effectuée immédiatement dans le chat actuel
- La tâche est déjà dans la roadmap
- La tâche est trop vague pour être utile (il faut un minimum de contexte)

## Création Autonome de Tâches

### Principe Fondamental

Tu **DOIS** créer des tâches de manière **autonome et automatique** lorsqu'au cours de ton travail (planification, discussion avec l'utilisateur, exploration du code, implémentation), tu identifies une tâche qui correspond aux critères de la section "Quand Créer une Tâche dans la Roadmap".

Ce comportement est **identique** à celui de la commande `/task`, mais déclenché de manière autonome sans intervention de l'utilisateur. La différence critique est que cette création est **non-bloquante** : tu crées la tâche puis reprends immédiatement ton travail comme si de rien n'était.

### Comportement Requis

Lorsque tu identifies une tâche future pendant ton travail, tu **DOIS** :

1. **Détecter automatiquement** :
   - Pendant l'élaboration d'un plan, tu identifies une dépendance ou un travail futur nécessaire
   - Pendant une discussion avec l'utilisateur, une tâche future est mentionnée ou suggérée
   - Pendant l'exploration du code, tu découvres un problème ou une opportunité qui nécessite un travail futur
   - Pendant l'implémentation, tu réalises qu'une amélioration ou un refactoring devrait être fait plus tard

2. **Créer la tâche silencieusement** :
   - Suivre **exactement** le processus décrit dans la section "Comment Créer une Tâche — Processus Complet" (Étapes 1 à 5)
   - Exécuter toutes les étapes sans pause ni interruption
   - Ne pas mentionner la création de la tâche à l'utilisateur sauf si :
     - La création échoue (fail-fast avec message d'erreur clair)
     - Le contexte nécessite explicitement de mentionner cette tâche pour la compréhension

3. **Reprendre immédiatement** :
   - Après avoir créé et sauvegardé la tâche dans la roadmap
   - Continuer exactement là où tu t'étais arrêté dans ton travail précédent
   - Reprendre tes todos en cours comme si rien ne s'était passé
   - La tâche créée sera traitée par un autre agent (via `/agent`) plus tard

### Exemples Concrets

**Exemple 1 — Pendant un plan** :
Tu es en train de planifier l'implémentation d'un système d'authentification. Pendant l'analyse, tu découvres que les performances pourraient être optimisées, mais cela nécessite une exploration approfondie qui sort du scope actuel.
→ **Action** : Créer automatiquement la tâche "Optimiser les performances d'authentification" avec priorité 3, puis continuer la planification de l'authentification comme si de rien n'était.

**Exemple 2 — Pendant une discussion** :
L'utilisateur mentionne "on pourrait aussi améliorer le système de cache plus tard" pendant que tu travailles sur une autre fonctionnalité.
→ **Action** : Créer automatiquement la tâche "Améliorer le système de cache" en capturant le contexte de la discussion, puis reprendre le travail sur la fonctionnalité en cours.

**Exemple 3 — Pendant l'exploration** :
Tu explores le codebase pour comprendre comment fonctionne un module. Tu découvres du code dupliqué qui devrait être refactorisé, mais ce n'est pas l'objectif actuel.
→ **Action** : Créer automatiquement la tâche "Refactoriser le code dupliqué dans [module]" avec les fichiers pertinents listés, puis continuer ton exploration initiale.

**Exemple 4 — Pendant l'implémentation** :
Tu implémentes une nouvelle fonctionnalité et tu réalises qu'un test d'intégration complet serait nécessaire, mais cela dépasse le scope de l'implémentation actuelle.
→ **Action** : Créer automatiquement la tâche "Créer des tests d'intégration pour [fonctionnalité]" en référençant les fichiers modifiés, puis continuer l'implémentation.

### Différence avec `/task`

La commande `/task` est utilisée par l'utilisateur pour créer explicitement une tâche. Les comportements sont techniquement identiques (même processus de création), mais diffèrent sur les points suivants :

**Création autonome (agent.mdc)** :
- **Déclenché automatiquement** par l'agent sans intervention utilisateur
- **Silencieux** : pas de message de confirmation sauf en cas d'erreur
- **Non-bloquant** : aucun changement de focus ou d'interruption du travail

**Commande `/task`** :
- **Interruption obligatoire et immédiate** : suspend TOUT travail en cours pour être traitée
- **Avec confirmation** : `✅ Tâche ajoutée (task-{id})` pour informer l'utilisateur
- **Reprise immédiate** : l'agent reprend le travail précédent après confirmation

### Gestion des Erreurs

Si la création de la tâche échoue (fichier roadmap.yaml invalide, dépendance manquante, etc.) :
- **Échouer explicitement** avec un message d'erreur clair à l'utilisateur
- Après avoir informé de l'erreur, **reprendre néanmoins** ton travail précédent
- Ne pas bloquer indéfiniment sur l'erreur de création de tâche

## Comment Créer une Tâche — Processus Complet

### Étape 1 : Identifier les Métadonnées

Avant de créer la tâche, identifier :

- **Titre descriptif** : Un titre clair et actionnable (ex: "Optimiser les performances d'authentification")
  - **IMPORTANT** : Le titre doit être unique pour éviter les collisions de noms de fichiers
- **Priorité** : De 1 à 5 (5 = plus haute priorité)
  - 5 = Critique/Blocant
  - 4 = Haute priorité
  - 3 = Priorité normale (défaut)
  - 2 = Nice-to-have
  - 1 = Faible priorité
- **Dépendances** : Liste des IDs de tâches qui doivent être terminées avant (peut être vide)
- **Contexte** : Pourquoi cette tâche existe, ce qui a été découvert, etc.

### Étape 2 : Générer le Nom de Fichier

1. Convertir le titre en format kebab-case (ex: "Optimiser les performances" → "optimiser-performances")
2. **IMPORTANT** : Vérifier que le titre est unique dans la roadmap pour éviter les collisions de noms de fichiers
3. Nom du fichier de tâche : `{titre-kebab-case}.md`
4. Nom du fichier de résultat : `rapport-{titre-kebab-case}.md`

Exemple :
- Titre : "Supprimer tous les serveurs MCP obsolètes"
- Fichier tâche : `supprimer-tous-les-serveurs-mcp-obsoletes.md`
- Fichier résultat : `rapport-supprimer-tous-les-serveurs-mcp-obsoletes.md`

### Étape 3 : Créer le Fichier de Tâche

Créer le fichier `.cursor/agents/{nom-fichier-tache}.md` avec **exactement** ce format :

```markdown
## Contexte

[Écrire en français une histoire narrative expliquant pourquoi cette tâche existe. Mentionner ce qui a été découvert, les problèmes identifiés, ou les opportunités qui justifient cette tâche. Utiliser un langage naturel et narratif, sans trop de détails techniques.]

## Objectif

[Écrire en français une description vague mais claire de ce qui doit être accompli. L'objectif doit être exploratoire, pas trop précis. Accepter le vague - la précision viendra avec la discussion lors de l'implémentation.]

Exemple : "Explorer la possibilité d'optimiser le système d'authentification pour améliorer les performances sous charge" plutôt que "Configurer un cache Redis avec paramètres spécifiques X, Y, Z".

## Fichiers Concernés

[Écrire en français. Lister DEUX catégories de fichiers/dossiers/recherches :]

### Du travail effectué précédemment :
- `chemin/vers/fichier1` : [Expliquer ce qui a été fait ou découvert dans ce fichier]
- `chemin/vers/fichier2` : [Expliquer ce qui est pertinent]

### Fichiers potentiellement pertinents pour l'exploration :
- `chemin/vers/fichier3` : [Expliquer pourquoi ce fichier pourrait être important]
- `chemin/vers/dossier/` : [Expliquer la pertinence]

### Recherches à effectuer :
- Recherche sémantique : "Comment sont gérées les performances dans le codebase ?"
- Recherche web : "Meilleures pratiques pour optimiser l'authentification JWT"
- Documentation : Lire `README.md` et `documentation/architecture.md`

### Fichiers de résultats d'autres agents (si pertinents) :
- `.cursor/agents/rapport-tache-precedente.md` : [Expliquer pourquoi ce rapport est utile]

**Fichier output pour le rapport final :**
- `.cursor/agents/rapport-{titre-kebab-case}.md`

## Instructions de Collaboration

[Écrire en français. **OBLIGATOIRE ET CRITIQUE** : Cette section doit être extrêmement directive et impérative. Tu DOIS spécifier que l'agent :]

- **EST INTERDIT** de commencer à implémenter quoi que ce soit immédiatement
- **DOIT** lire EXHAUSTIVEMENT tous les fichiers listés dans "Fichiers Concernés" avant toute action
- **DOIT** effectuer toutes les recherches sémantiques et web mentionnées
- **DOIT** lire le README et toute documentation pertinente
- **DOIT** atteindre une compréhension approfondie du contexte et du projet avant toute discussion
- **DOIT** discuter avec l'utilisateur pour clarifier les attentes précises, poser des questions sur les contraintes techniques, et établir un plan d'action détaillé ensemble
- **DOIT** écrire le rapport final dans le fichier output mentionné après avoir terminé
- Seulement APRÈS avoir complété cette exploration exhaustive et cette planification collaborative, peut commencer toute implémentation

Emphasizer que l'exploration est OBLIGATOIRE, pas optionnelle.
```

**IMPORTANT** : Tous les fichiers de tâches doivent suivre **exactement** ce format avec les 4 sections obligatoires.

### Étape 4 : Ajouter l'Entrée dans roadmap.yaml

Lire le fichier `.cursor/agents/roadmap.yaml` et ajouter une nouvelle entrée dans la liste `tasks` :

```yaml
- id: "task-{unique-id}"  # Générer un ID unique (ex: task-1, task-2, etc.)
  title: "Titre descriptif de la tâche"
  priority: 3  # 1-5
  dependencies: []  # Liste d'IDs de tâches ou []
  task_file: "{nom-fichier-tache}.md"
  output_file: "rapport-{nom-fichier-tache}.md"
  deadline: null  # Optionnel
```

**Génération d'ID unique** :
- Si le fichier roadmap.yaml existe déjà, lire toutes les tâches existantes
- Identifier le plus grand ID numérique utilisé (ex: si task-5 existe, le prochain est task-6)
- Utiliser le format `task-{numero}` pour garantir l'unicité

**Validation** :
- Vérifier que le fichier `task_file` existe (que tu viens de créer)
- Vérifier que les dépendances mentionnées existent dans la roadmap (si des dépendances sont spécifiées)
- Si validation échoue → **ÉCHOUER EXPLICITEMENT** avec un message clair

### Étape 5 : Sauvegarder

Sauvegarder le fichier `roadmap.yaml` avec la nouvelle entrée ajoutée.

## Exemple Complet

Supposons que tu travailles sur l'authentification et découvres des problèmes de performance. Tu veux créer une tâche pour optimiser plus tard.

**Étape 1 - Métadonnées** :
- Titre : "Optimiser les performances d'authentification"
- Priorité : 4
- Dépendances : []
- Contexte : Problèmes de performance découverts lors des tests

**Étape 2 - Noms de fichiers** :
- Fichier tâche : `optimiser-performances-auth.md`
- Fichier résultat : `rapport-optimiser-performances-auth.md`

**Étape 3 - Créer le fichier de tâche** :
- Créer `.cursor/agents/optimiser-performances-auth.md` avec les 4 sections (vérifier l'unicité du titre)

**Étape 4 - Ajouter dans roadmap.yaml** :
```yaml
- id: "task-1"
  title: "Optimiser les performances d'authentification"
  priority: 4
  dependencies: []
  task_file: "optimiser-performances-auth.md"
  output_file: "rapport-optimiser-performances-auth.md"
  deadline: null
```

## Points d'Attention

- **Fail-Fast** : Si roadmap.yaml n'existe pas, le créer avec la structure de base (`version: "1.0"`, `tasks: []`)
- **Format cohérent** : Tous les fichiers de tâches doivent suivre exactement le même format
- **Validation** : Valider que les dépendances existent avant d'ajouter la tâche
- **Français** : Tout le contenu des fichiers de tâches doit être en français
- **Unicité** : Garantir que les IDs de tâches sont uniques

## Intégration avec le Workflow

Cette règle fonctionne en complément de :
- La commande `/agent` qui permet de sélectionner et traiter une tâche
- Les autres règles existantes (peut être combinée avec `agent.mdc`, `debug.mdc`, etc.)

Quand tu identifies une tâche future, crée-la immédiatement dans la roadmap plutôt que de la noter mentalement ou de l'oublier.
